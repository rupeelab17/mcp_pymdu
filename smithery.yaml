runtime: "container"
build:
  dockerfile: "Dockerfile"           # Path to your Dockerfile
  dockerBuildPath: "."               # Docker build context
start:
  command: ["micromamba", "run", "-n", "mcp_pymdu", "/bin/bash", "-c", "fastmcp run mcp_pymdu/server.py --transport stdio"]
  port: 8000