version: 1
start:
  command: ["python", "-m", "mcp_pymdu.server.py"]
  build:
    dockerfile: "Dockerfile"           # Path to your Dockerfile
    dockerBuildPath: "."               # Docker build context
  port: 8000
  startCommand:
    type: "http"